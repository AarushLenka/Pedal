<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Base application theme. -->
  <style name="Theme.Pedal" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    <!-- Primary brand color. -->
    <item name="colorPrimary">@color/gruvbox_bg</item>
    <item name="colorPrimaryVariant">@color/gruvbox_bg</item>
    <item name="colorOnPrimary">@color/gruvbox_fg</item>
    <!-- Secondary brand color. -->
    <item name="colorSecondary">@color/gruvbox_orange_dark</item>
    <item name="colorSecondaryVariant">@color/gruvbox_orange_light</item>
    <item name="colorOnSecondary">@color/gruvbox_fg</item>
    <!-- Status bar color. -->
    <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    <!-- Customize your theme here. -->
    <item name="android:windowBackground">@color/gruvbox_bg</item>
    <item name="android:textColor">@color/gruvbox_fg</item>
  </style>
</resources>